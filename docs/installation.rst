.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install course_backup

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv course_backup
    $ pip install course_backup
